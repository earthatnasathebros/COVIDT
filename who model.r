#step 1 : prep and import data
file.path("C:Downloads\nz-covid-data-main (1).zip\nz-covid-data-main\locations-of-interest\august-2021", "locations-of-interest.csv"))
                mydata <- read.csv('https://github.com/minhealthnz/nz-covid-data/blob/main/locations-of-interest/august-2021/locations-of-interest.csv')	
rxRoxCurve("ArrDel15",
                predVarNames = c ("ArrDel15_Pred", "ArrDel15_Pred_Logit"),
                data = test,
                title = "ROC curve - Logistic regression

url = 'https://github.com/minhealthnz/nz-covid-data/blob/main/locations-of-interest/august-2021/locations-of-interest.csv'
write int (current,passport);
writeln interger;HP7559

itemtype=RECORD
	trans: INTEGER=itemtype record
	arrive: INTEGER:=HP7559
	write int (current,passport);
	writeln interger;HP7559
covid19_dTree<-rxDTree(modelFormula, data = train, reportProgress =update)
      eventinfo=:HP7559
	PRINT; time : INTEGER;
	CASE etype : events .PASSPORT
 	eventinfo=:HP7559
 url = 'https://github.com/minhealthnz/nz-covid-data/blob/main/locations-of-interest/august-2021/locations-of-interest.csv'
     rpath =_url(url)}
 current,passport_tracker_county_geo = function() {
   url = "'https://github.com/minhealthnz/nz-covid-data/blob/main/locations-of-interest/august-2021/locations-of-interest.csv"
   rpath = s2p_cached_url(url)
   dat = data.table::fread(rpath)
   dat$countyfips=integer_to_fips(dat$countyfips)
   dat$statefips = integer_to_fips(dat$statefips)
   dat
 }

 dat = data.table::fread(rpath)
 print;(tt$"passport)
}
